# IMAGE-BRIEF — P4 render + transport + QA agent

Read whole: ADS-BRIEF-COMMON.md, IMAGE-TRANSPORT.md, the skill SKILL.md §A17, §A22 and references/production.md ("Per-image generation handoff", "Check individual images"), then every prompts/*.txt (15 files) and fragments/C1..C5.json (the ledger entries: id, vehicle, aspect, exact text manifest inside prompt_file, product_role).

TOOLS (load with ToolSearch `select:` first): mcp__Higgs__models_explore (action recommend once, for "designed ad with exact text at 1:1/4:5, product reference element" and for "candid phone photo, no text"), mcp__Higgs__generate_image_batch / generate_image, mcp__Higgs__jobs_wait, mcp__Higgs__show_generation_by_ids, mcp__Higgs__balance, mcp__Higgs__sandbox_exec. Step 13 used `nano_banana_2` (strong exact-text rendering) with reference elements; keep it unless models_explore recommends otherwise for the no-text natives. Balance at start ≈1,977 cr; HARD CAP for this job 80 cr (≈15 renders + one repair round). Check balance before and after; stop at the cap and report.

DO, per ad id (15: C1-V1 … C5-V3; launch ids first: C1-*, C2-*, C3-*, then C4-*, C5-*):
1. Generate at the exact aspect from the ledger (1:1 or 4:5). V1 ids pass the product reference element 9597a0b7-58a4-42f0-bdc9-1b2663b1ce17 (and environment de8b79fa-3285-4487-8076-c22e091bfd4d when the prompt asks for the back room); V2/V3 pass NO reference element. Batch ≤8 at a time; jobs_wait; record job_id, model, width×height, rawUrl for each.
2. Transport per IMAGE-TRANSPORT.md into images/<id>.jpg (800 px proof copy, md5 verified, dimensions from `identify` recorded). Keep every base64 line intact; one image per sandbox call.
3. QA with the Read tool on the local JPEG: every manifest string present and spelled exactly (character by character against the prompt's quoted strings), no invented text, no logos/badges/stars, product fidelity (V1), the surface reads native (V2/V3: no ad bands/buttons/brand colours), ratio matches, phone-size readability (Squint/Phone/Thumb-Stop). Record qa.status pass|fail + notes.
4. Repair: a failed render gets ONE regeneration with the likely trigger removed or the text simplified (never change the manifest meaning; if a string will not render, shorten it and log the change for the writer's ledger). Keep the failed job_id in notes. A second failure stays `fail` with the reason; never mark pass to reach the count; never create placeholder files.
5. Write fragments/images.json (.partial then rename): {"<id>": {"job_id","model","width","height","rawUrl","local_file":"images/<id>.jpg","md5","aspect_ok":true|false,"qa":{"status","notes"},"repair_of": job_id|null}} for all 15, plus "spend_credits" (balance before − after) and "balance_after".
6. Write contact-sheets/contact-sheet.html (.partial then rename): a grid of the 15 local proof JPEGs (relative paths) with id, vehicle, aspect, actual pixels, qa status, and the full-res rawUrl link; plus contact-sheets/closest-pairs.md: the nearest-duplicate pairs by layout/medium/meaning (references/originality.md closest-pair audit) and what differs.

RULES: images carry only the text the prompt quotes; V3 story images carry no product, brand, price or text (unless N9/N10 wording is quoted); never invent packaging; concurrency ≤8; log every job (also failures) in fragments/images.json. No other tools, no other spend. Time box 50 minutes / 130 tool calls; on expiry rename what is complete and reply PARTIAL with the ids left.

REPLY with ONE line: `DONE|PARTIAL · P4-IMAGES · rendered n/15 · transported n/15 · qa pass n / fail n / pending n · repairs n · credits used X (balance Y) · files: [fragments/images.json, contact-sheets/contact-sheet.html, contact-sheets/closest-pairs.md, images/*.jpg] · ids left (PARTIAL only)`
