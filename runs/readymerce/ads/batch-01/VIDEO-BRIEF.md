# VIDEO-BRIEF — P5 render agent for the C1 VF1 UGC confessional (45 s, 9:16)

Read whole: ADS-BRIEF-COMMON.md, IMAGE-TRANSPORT.md, the skill SKILL.md Part C (C0–C11), ads/C1.md §VIDEO (the C8 script table, the 3 hook variants, the AI shot prompts incl. the character-reference prompt, edit notes), fragments/images.json (for the product render ids, if any shot needs the store-on-laptop).

TOOLS (ToolSearch `select:` first): mcp__Higgs__balance, mcp__Higgs__models_explore (action get, model_id seedance_2_0 → read medias[].roles, params), mcp__Higgs__generate_image (character reference, model nano_banana_2, 9:16 or 3:4), mcp__Higgs__generate_video_batch (model seedance_2_0, mode std, 720p, 9:16, generate_audio true, duration ≤15), mcp__Higgs__jobs_wait, mcp__Higgs__show_generation_by_ids, mcp__Higgs__sandbox_exec (ffmpeg concat + caption burn-in + thumbnail), mcp__Higgs__media_upload + media_confirm (to keep the stitched MP4 in the workspace), mcp__Higgs__job_display. HARD CAP 260 credits for this job (preflight: 15 s seedance_2_0 720p std = 67.5 cr; 3 clips ≈ 203 cr; one repair clip allowed inside the cap). Check balance before/after; stop at the cap and report.

DO:
1. Character lock (C7b): generate ONE character reference image from the script's character-reference prompt (the composite AV-07 owner: late 40s, cardigan, reading glasses, back room of a small independent shop; synthesized, never presented as a customer). Record job_id/rawUrl. Use it as `image_references` on every talking shot.
2. Split the 45 s C8 script into 3 shots of ≤15 s each along the beat boundaries (hook + lead / failed fixes + aha / product + proof + CTA). For each shot write the Seedance prompt in C7b order (subject from the reference, the exact spoken line in quotes to be lip-synced as native audio, action, setting, camera "handheld iPhone selfie, eye level, slight movement", lighting, style "UGC, natural light, slight grain", 9:16) + negative prompt (no on-screen text, no captions, no logos, no watermarks, no warped hands, no studio lighting, no slow motion). Spoken words per shot ≈ 2.5 words/s (≤37 words per 15 s). Use hook variant H1 in shot 1. Product never appears before problem recognition (C0.5); the $500 line and the 3-word CTA are the final spoken line.
3. Submit the 3 shots with generate_video_batch, jobs_wait in one group, show_generation_by_ids once. Record job_id, duration, rawUrl per shot.
4. QA: in the sandbox, `ffprobe` each clip (duration, size), extract 3 frames per clip (0 s, mid, last) as 480 px JPEGs, transport the 9 frames per IMAGE-TRANSPORT.md into videos/frames/C1-V-shot<k>-<n>.jpg (one sandbox call per frame or pack 3 frames per call if the base64 stays under ~60 KB), and look at them with the Read tool: identity consistent across shots, lip movement present, no text/logos, framing native. Transcribe the native audio in the sandbox with faster-whisper (python3, model "base") and compare to the script lines; log word-level mismatches. A shot that fails identity or says the wrong words gets ONE regeneration inside the cap.
5. Stitch in the sandbox: `ffmpeg` concat of the 3 clips → C1-V-45s.mp4; burn the on-screen text cards from the script's On-screen text column as captions (Montserrat bold, 2–4 words per line, inside the central safe zone: top 14% and bottom 20% clear) and the end card (the 3-word outcome + "$500 Build · readymerce.com") for the last 2 s; export C1-V-45s-captioned.mp4. Upload both via media_upload (call it before the producing command; PUT from the same sandbox command) and media_confirm; record media_ids. Transport a 480 px thumbnail of the end card to videos/C1-V-endcard.jpg for the operator proof.
6. Write videos/C1-V.json (.partial then rename): {character_ref:{job_id,rawUrl}, shots:[{k, job_id, rawUrl, duration_s, spoken_line, transcript, mismatches[], qa:{status,notes}}], stitched:{media_id_raw, media_id_captioned, duration_s, safe_zone_ok}, frames:[paths], credits_used, balance_after, hook_variants_unrendered:["H2","H3"], notes}.

RULES: never invent facts in spoken lines beyond ads/C1.md; the lint in ADS-BRIEF-COMMON.md is binding; no other spend; time box 45 minutes / 110 tool calls; on expiry keep what is complete and reply PARTIAL with the resume point.

REPLY with ONE line: `DONE|PARTIAL · P5-VIDEO-C1 · shots n/3 rendered · transcript match n/3 · identity PASS|FAIL · stitched Y/N (media ids) · frames n · credits used X (balance Y) · files: [..] · resume point (PARTIAL only)`
