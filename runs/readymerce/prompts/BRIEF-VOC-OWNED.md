# SCOPE ADDENDUM — VOC-OWNED-<US|UK> (STEP 06 HARVEST over the operator's own sales-call transcripts)

You run STEP 06 in HARVEST mode with `SCOPE: OWNED-<US|UK>`. Your corpus is NOT the web: it is the operator's owned sales-call transcripts (`[R-OWNED]`, corpus `OWNED_PROOF`), supplied for objection / belief / desire / situation analysis. Everything in STEP 06 §3 (definitions), §4.1 (the Q-record columns, exact order), §4 tags, §6 (validation) applies unchanged. Only the source and the file names change:

**Source units:** one call = one source unit. Files: `/home/user/main/runs/readymerce/inputs/transcripts/`. OWNED-US = files named `Transcription-*Call with +1*.txt` and `Transcription-*Call with Mark*.txt` (US numbers, ≈151 files). OWNED-UK = files named `*call <id> 44*.txt` / `*call <id> 353*.txt` / `*call <id> 49*.txt` and `Transcription-*Call with +44*.txt` (UK/IE/DE numbers, ≈129 files). Two transcript formats exist: (A) `Name HH:MM:SS` line then the utterance; (B) `[Speaker N] (m:ss - m:ss)` then the utterance. Use python to parse speaker turns for BOTH formats.

**Speaker roles (code + one read per call):** the SALES REP is the speaker who says they are calling from Readymerce / "Redimmers" / about "the registration you did", who pitches the store, plans, prices, deposits; the PROSPECT is the other party. Only PROSPECT utterances become `Q-O-####` records (`unit_type: transcript`, `source_type: owned_dm` is wrong — use `source_type: operator_paste` with `notes: sales-call transcript`), `corpus: OWNED_PROOF`, `is_purchaser_source: N` unless the prospect states a purchase/deposit on the call (then Y). SALES-REP statements about the offer (plans, prices, deposits, what is included, guarantees, timelines, objection handling lines) become `C-####` contextual rows (`corpus: OWNED_PROOF`, `source_type: expert`, `notes: sales-rep pitch — contextual, never VOC, never in a language bank`) — 12 reads them as the operator's CURRENT offer benchmark and 11 as the current objection handling; cap 40 C rows per harvester, the most-repeated pitch lines first, with a count of calls they recur in.

**Privacy:** `unique_author_id` = `O-` + first 8 hex of sha1(filename); never write a phone number, surname or employer name into any field; a first name the prospect gives may stay only inside `exact_passage` if it is not a surname. `source_url` = the file path relative to `/home/user/main/runs/readymerce/inputs/transcripts/`. `thread_id` = the filename stem. `date_posted` = the date in the UK filename (`YYYYMMDD` in the third token) or `2026-09 (SAMPLE — file date)` for US files. `community` = `Readymerce sales calls — <US|UK>`. `harvester_id` = `VOC-OWNED-<US|UK>`. `recipe_id` = `RC-OWNED`. `tier` = `[R-OWNED]`.

**Owned-specific fields (inside the existing columns, never new columns):** put in `life_context`: `call_market: US|UK; employment: <as stated or NULL>; funds: <as stated or NULL>; household: <as stated or NULL>`. Put in `notes`: `call_outcome: PAID | DEPOSIT | BOOKED-FOLLOWUP | DECLINED | HUNG-UP | UNKNOWN; lead_quality_marker: <the concrete signal, e.g. "no job, using partner's income", "asked what the company does", "wants to invest $10k", or NULL>`. `speaker_stage` per §3 (sufferer/seeker/recent_buyer/…); `awareness_stage` per §6.6 (what the passage itself demonstrates about online-business solutions; `awareness_ref` = "done-for-you store" / "dropshipping" / "Amazon FBA" / … as named).

**Tags:** the closed §4.1 vocabulary. Especially: `objection` (every reason not to buy, verbatim, as the prospect said it), `belief`, `skepticism` (with the reason), `desire` / `deeper_hope` / `if_only` / `micro_dream` (why they registered, what they picture), `trigger` / `made_them_act` (what made them register / what would make them pay), `spend` (money they have, have spent, can raise, or refuse), `failed_solution` (things they tried: other programs, courses, Amazon FBA, gurus, a previous store) with `solution_status`, `scene`, `identity`, `fear`, `tradeoff`, `almost_stopped`, `refuses`, `tired_of_hearing`, `enemy_blame`, `question_asked`, `hyper_responsive` (top 20% longest prospect talk). `population_tags[]` / `population_id` from 01's `CP-##` list (read `01-HANDOFF.json` first) — including the operator's two hypothesis populations if 01 minted them — and `pb_ids[]` from 01's `PB-##`; NULL when the passage does not place it; never infer sensitive facts.

**Floors (scoped, this harvester):** ≥70 Q-O records (target 120) from ≥40 distinct calls · `objection` ≥20 · `belief` ≥10 · `skepticism` ≥10 · `desire`+`deeper_hope` ≥12 · `spend` ≥10 · `trigger`+`made_them_act` ≥8 · `failed_solution` ≥6 · `scene` ≥8 · C rows ≤40. Reading order per call: the prospect's longest turns first, then every turn carrying money, family, job, time, "tried", "scam", "not sure", "call me back", "my wife/husband", "kids", "retire".

**Method (batch the reads):** Step 0 lock (print the floor table above, the file list with byte sizes, `01-HANDOFF.json` PB/CP ids). Step 1 in code: parse all files → per call: speaker map, rep vs prospect, prospect word count; save `datasets/owned-<US|UK>-turns.jsonl` (one prospect turn per line with call id, t, text) and a ranking CSV. Step 2: read prospect turns call by call in rank order (longest prospect talk first) in slices of ≤12 calls per `cat`/python print, writing 0–5 records per call IMMEDIATELY to `06-partials/06-VOC_MASTER.OWNED-<US|UK>.csv.partial` (§4.1 columns, `csv.QUOTE_ALL`), printing the counter after each call and `CHECKPOINT url_index=n records=m cost=$0` every 10 calls to `06-partials/06-HARVEST-LOG.OWNED-<US|UK>.md`; every call ends with a records line or `0 records — reason`. Continue until floors are met AND ≥40 calls read, or the box ends. Step 3: C-#### rows from the rep's most repeated pitch lines (count calls). Step 4: per-harvester ledgers per tag / per CP (unique authors = distinct calls), `hyper_responsive` flag, manifest rows (unit_id = call id, lane owned_paste). Step 5: scoped COVERAGE STATEMENT (`COVERAGE OWNED-<US|UK>: records n/70 · calls read n/N · tags below floor: [..|NONE] · C rows n · resume: url_index=n · weakest link: …`), rename the CSV (drop `.partial`), write `06-partials/06-PROGRESS.OWNED-<US|UK>.md` and `06-partials/06-HANDOFF.OWNED-<US|UK>.json` (the HARVEST partial keys per STEP 06 §7 plus `"scope":"OWNED-<US|UK>"`, `"calls_read"`, `"calls_total"`, `"c_rows"`, `"call_outcome_counts"` as a value).
No web calls, no scrapers, $0 spend. Box: 30 min / 100 tool calls — most of your calls are Bash reads; batch them.
